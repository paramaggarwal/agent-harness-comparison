{
  "name": "agent-harness-comparison",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev --extends docus",
    "build": "nuxt build --extends docus",
    "generate": "nuxt generate --extends docus && node scripts/post-generate.mjs",
    "preview": "nuxt preview --extends docus"
  },
  "dependencies": {
    "better-sqlite3": "^12.5.0",
    "docus": "latest",
    "nuxt": "^4.2.2"
  }
}
